#include <stdio.h>
int validarFlotante(int numero, float* pResultado){

	int retorno = -1;
	int bufferint;

		printf("Ingrese un numero flotante: ");
		scanf("%d", &bufferint);

	if(pResultado != NULL &&){
		*pResultado = numero;
		retorno = 0;
	}
	return retorno;
}

