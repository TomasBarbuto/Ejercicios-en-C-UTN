#include <stdio.h>
int validarFlotante(int numero, float* pResultado){

	int retorno = -1;

	if(pResultado != NULL){
		*pResultado = numero;
		retorno = 0;
	}
	return retorno;
}

int validarNumeroPar (int numero, int* pResultado){


	return retorno;
}

