#include <stdio.h>
int validarFlotante(int numero, float* pResultado){

	int retorno = -1;

	if(pResultado != NULL){
		*pResultado = numero;
		retorno = 0;
	}
	return retorno;
}

int validarNumeroPar (int numero, int* pResultado){

	int retorno = -1;
	int bufferint;

	if(*pResultado != NULL){
		if(bufferint % 2 == 0){

		}
	}
	return retorno;
}

