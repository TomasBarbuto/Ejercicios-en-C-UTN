#include <stdio.h>
int validarFlotante(float numero, float* pResultado){

	int retorno = -1;

	numero = str(len(numero));

	if(pResultado != NULL && numero >= 0){
		numero = float(numero);
		*pResultado = numero;
		retorno = 0;
	}


	return retorno;
}

